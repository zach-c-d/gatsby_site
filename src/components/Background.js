import styled from 'styled-components';
import { background_Red } from '../utilities/colors';

const Background = styled.div`
  position: static;
  left: 0;  
  width:100vw;
  height: 100vh;
  background: ${background_Red};
`

export default Background;